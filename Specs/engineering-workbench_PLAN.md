# Plan: Engineering Workbench

## Proposed Vertical Slices

1. **The near miss opens the responsible Prompt in a focused inspector**
   - Blocked by: Player Experience #3-#4, Content Registry #1-#3,
     Instruction #1-#4, Context #1-#2
   - Adds Workbench route/mode, one Park Developer presence, operational anchor,
     exact artifact inspection, source/clauses distinction, context composition,
     dependencies, tools, tradeoffs, and version history.
   - Tests: exact fields, historical version, no prose execution implication,
     route identity, keyboard sections, and long-text reflow.
   - Browser proof: follow incident to artifact, inspect it, and return.

2. **Two feeding approaches compare across behavior and context**
   - Blocked by: #1, Instruction #5
   - Adds semantic comparison, aligned readable/behavioral changes, context and
     dependency deltas, verification/failure differences, evidence links, and
     no universal ranking.
   - Tests: additions/removals, duplicated Policy, missing verification, stale
     dependency risk, irrelevant load, and unsupported-quality claim rejection.
   - Browser proof: compare self-contained and modular approaches at 200% text.

3. **Minimum composition fixes the missing maintenance context**
   - Blocked by: #1-#2, Context #2
   - Adds bounded context-route/component editor, previewed capacity delta,
     validation, goal, base version, and draft candidate structure.
   - Tests: valid route, missing dependency, conflict, preview accuracy, prose-
     only no-op, cancel, and unchanged production.
   - Browser proof: add the maintenance route and see the exact preview.

4. **Park Developer work creates an immutable candidate version**
   - Blocked by: #3, Economy capability/quote port
   - Adds one Developer capability profile, available work, request lifecycle,
     quote acceptance, completion, candidate registration, feedback linkage, and
     concise communication.
   - Tests: capability gate, work transitions, immutable completed version,
     revision request, cancellation rules, and no production deployment.
   - Browser proof: commission the opening fix and inspect its change summary.

5. **Engineering Handbook preserves encountered concepts without becoming context**
   - Blocked by: #1, Curriculum Content Handbook entries
   - Adds term/example unlocks, search/filter, incident links, visual grammar
     reference, decorative theming, and hard separation from context sources.
   - Tests: unlock after experience, search, stable links, no Context adapter,
     keyboard use, and damage never overlaps content.
   - Browser proof: open the relevant context entry from the incident, then
     confirm it cannot be selected as Agent Knowledge.

6. **Workbench validation gate**
   - Blocked by: #1-#5
   - Adds broad artifact fixtures, rendered diff tests, architecture lint,
     accessibility checks, and full validation.
   - Browser proof: inspect, compare, compose, commission, request changes,
     Handbook, keyboard-only, text scaling, and causal return.
