# Phase 7 verification record

Verified in connected Chrome on 2026-08-19 with high contrast, 150% text, and
keyboard activation paths. `npm run validate` passed environment validation,
typecheck, lint, architecture lint, shell tests, all 187 tests, and production
build/PWA generation.

## Browser evidence

- Review/Deployment: attached real failed Eval evidence, confirmed default
  blocking, explicitly accepted non-mandatory risk, deployed, reverted as a new
  record, redeployed, and returned by keyboard to the originating incident.
- Incident Response: inspected the full plan/limitations, proved stale failure
  and idempotent activation, advanced the exact lifecycle, and retained the
  persistent `engineering-unresolved` result and unchanged engineering
  boundaries.
- Persistence: wrote and promoted real IndexedDB saves, autosaved at a safe
  checkpoint, listed stable metadata, exported locally, quarantined tampering,
  migrated with original backup, refused unconfirmed deletion, restored an
  exact save, and replayed its historical trace.
- Economy: settled the first full day, observed availability before purchase,
  purchased the Context Optimization action, placed and removed the approved
  plushie asset, and retained exact inventory/placement history with zero
  mechanical multiplier.
- Player Experience: escalated and skipped guidance by keyboard, staged the
  grouped near miss, inspected the real synchronized Eval/Replay projection,
  restored the exact Gate Beta event, and verified memorable/later/reduced-
  motion retention with persistent Excluded/Compacted/Externalized records.
- Curriculum: unlocked the Handbook after the incident and completed the novel
  Ankylosaurus/Gamma transfer by keyboard, with opening guidance withheld and
  optional help unavailable until after the missing Context was observed.
- Acceptance invariants: compared a prose-only artifact, exercised Context
  growth/Strict/Keep Newest/Priority/Externalize paths, verified no Context
  overflow reached Instruction, and verified exact replay/pinned history.
- Offline core play: loaded the production PWA, confirmed `offline-ready`,
  stopped the preview server, reloaded `/park` successfully from the service
  worker cache, and completed Tria's feeding by keyboard with the server still
  stopped.

## External human gates

The authored 300-second opening is ready, but representative-newcomer timing
and experienced Agent-user credibility cannot be supplied by automation or an
AI self-assessment. The exact participant and evidence requirements are
recorded as `[!]` items in `checklist.md`.
